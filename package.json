{
  "scripts": {
    "upload": "rm -rf dist/ botasaurus.egg-info  MANIFEST && python setup.py sdist && pip install -e . && python -m twine upload dist/* --verbose",
    "install": "rm -rf dist/ botasaurus.egg-info  MANIFEST && python setup.py sdist && pip install -e ."
  }
}