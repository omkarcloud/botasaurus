{
  "scripts": {
    "upload": "rm -rf dist/ botasaurus.egg-info  MANIFEST && python setup.py sdist && pip install -e . && python -m twine upload dist/* --verbose && pip install .",
    "install": "rm -rf dist/ botasaurus.egg-info  MANIFEST && python setup.py sdist && pip install -e .",
    "local-install": "pip install ."
  }
}