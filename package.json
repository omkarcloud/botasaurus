{
  "scripts": {
    "upload": "rm -rf dist/ bose.egg-info && python setup.py sdist && pip install -e . && python -m twine upload dist/*",
    "install": "rm -rf dist/ bose.egg-info  && python setup.py sdist && pip install -e ."
  }
}